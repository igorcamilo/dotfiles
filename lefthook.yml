pre-commit:
  commands:
    secrets:
      run: gitleaks protect --staged --redact
    no-plaintext-secrets:
      glob: "secrets/**"
      run: |
        echo "Refusing to commit plaintext secret file(s): {staged_files}" >&2
        exit 1
